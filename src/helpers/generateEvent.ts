import { RealEvent, Zone } from "../types";

export const generateEvent = (
  zones: Zone[],
  maxEventDurationMs: number
): RealEvent => {
  // TODO: implement
  return {
    plate: "",
    gateEvents: [],
  };
};
